# Web scraping a youtube comment 
This project allows you to extract comments from YouTube videos using Python.

## 📌 Features
- Extracts all top-level comments
- Saves comments to a CSV file
- Can be modified for replies or like count
- Useful for NLP, sentiment analysis, or dataset creation

## ⚙️ Requirements

Install dependencies with:
```bash
pip install -r requirements.txt
